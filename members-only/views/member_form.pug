extends layout 

block content 

    h2 #{title}

    form(method='POST' action='')
        div.form-group
            label(for='membercode') Member Code
            input#membercode.form-control(type='password' name='membercode' required= 'true')
        button.btn.btn-primary(type='submit') Submit 